/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Plain <img> is used for flags (external CDN), so next/image config isn't needed.
};

module.exports = nextConfig;
